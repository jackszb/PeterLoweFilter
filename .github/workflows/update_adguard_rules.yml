name: Update AdGuard Rules

on:
  schedule:
    - cron: '0 0 * * *'  # 每天午夜触发
  workflow_dispatch:   # 支持手动触发

jobs:
  update-rules:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Download AdGuard filter list
      run: |
        curl -o filter.txt https://raw.githubusercontent.com/AdguardTeam/HostlistsRegistry/main/filters/general/filter_3_PeterLoweFilter/filter.txt

    - name: Extract domains from the filter list
      run: |
        grep -Eo '([a-zA-Z0-9.-]+\.[a-zA-Z]{2,})' filter.txt | sort | uniq > domain_list.txt

    - name: Generate domain_list.json for sing-box
      run: |
        domains=$(cat domain_list.txt | jq -R . | jq -s .)
        json_content="{\"version\": 3, \"rules\": [{\"domain_suffix\": $domains}]}"

        # 使用 jq 格式化 JSON 输出
        echo "$json_content" | jq . > domain_list.json

    - name: Commit and push changes
      run: |
        git config user.name "GitHub Actions"
        git config user.email "actions@github.com"
        git add domain_list.txt domain_list.json
        git commit -m "Update domain list and JSON"
        git push
